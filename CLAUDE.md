# Chat2Events

从企业微信会话存档的群聊日志中抽取**结构化业务事件**，并产出群维度 / 客服维度指标，落库到 MySQL。

不是聊天机器人，不是问答系统。跑批 T+1，跑完即退出，**没有常驻服务**。

**webUI 是唯一的旁路，且只读**：给客服主管看指标、下钻原文。它与跑批完全解耦 —— 只从 MySQL 和 ① 的端口取数，不写任何表、不调模型。跑批不知道它存在。

## 七个阶段

```text
OSS 月文件 ──① pull（索引表 + HTTP Range）──> ./data/raw/   OSS 的字节级镜像
                          │
  ┌───────────────────────┴───────────────────────────────────┐
  │ ① 摄取 ingest  ＋  ② 会话 conversation                     │
  │   唯一认识上游字段名和路径布局的地方（DuckDB 盖在本地 NDJSON 上）│
  │   list_rooms / read_room / read_by_ids                     │
  └───┬─────────────────────────────────────────┬─────────────┘
      │ Conversation                            │ read_by_ids
      │ msgs + msg_counts，一趟出                │ 按 msg_id 取原文
      ▼                                         │
  ③ 抽取 extract     端口 SegmentModel          │
      │              Live / Recording / Replay   │
      ▼ EventDraft                               │
  ④ 装配 assemble    无端口 —— 溯源守卫，不可替换 │
      │                                          │
      ▼ Event（只有事实列）                       │
  ┌───┴──────────────────┐                       │
  ▼                      ▼                       │
⑤ 分类 classify        ⑥ 指标 metrics            │
  端口 Classifier        纯函数                   │
  └───┬──────────────────┘                       │
      ▼                                          │
  ⑦ 落库 store ───> MySQL ───> webUI（只读）◀────┘
                              客服主管 · 指标 + 下钻原文
```

| # | 阶段 | 模块 | 端口 | 出口类型 | 接缝真实性 | Rust 现状 |
|---|---|---|---|---|---|---|
| ① | **摄取** | `pull.rs` ＋ `ingest.rs` | 无（契约写在 `ingest.rs` 顶上的模块文档注释里） | `Message` | 1 个适配器 = **假想**接缝 | ✅（`pull.rs` ＋ `ingest.rs`） |
| ② | **会话** | 同 ①，不独立成模块 | `read_room()` | `Conversation` | — | ✅ |
| ③ | **抽取** | `extract.rs` | `SegmentModel` | `EventDraft` | 3 个适配器 = **真**接缝 | ⚠️ 只有冒烟 |
| ④ | **装配** | `extract.rs::assemble` | **无，且不该有** | `Event` | 溯源守卫 | ❌ |
| ⑤ | **分类** | `classify.rs` | `Classifier` | `type` | 2 个（v0 / v1）= **真**接缝 | ❌ |
| ⑥ | **指标** | `metrics.rs` | 无 | 指标行 | 纯函数 | ❌ |
| ⑦ | **落库** | `store.rs` | 无（已排除） | — | MySQL 是唯一目标 | ❌（连接池已建） |

另有 `daily.rs` —— 跑批那一轮的**编排**（① → ③④ → ⑤⑥ → ⑦）。它不是一个阶段，
是把七个阶段串起来的进程。`main.rs` 只负责读配置、起日志、建资源、调 `daily::run`。


**端口判据：一个适配器 = 假想接缝，两个 = 真接缝。** 只有 ③ 和 ⑤ 今天就有第二个实现，所以只有它们值得写 trait。**① 明确不写 `MessageSource` trait** —— 契约是**文字**的价值，写成 trait 壳只多一处「改签名要改两处」的负担，`ingest.rs` 顶上那段模块文档注释（`//!`）就是端口本身。**④ 明确不给端口** —— 它是承重不变量 6（溯源）的守卫，给它接缝等于给溯源留绕过口。**② 有阶段名但不独立成模块**（分组必须下推给源）：**五个阶段，四个模块。**

各模块内部、端口上什么不许出门 → `docs/architecture.md`。

---

## 文档地图

| 文档 | 装什么 | 什么时候读 |
|---|---|---|
| **`CLAUDE.md`**（本文件） | 全局地图 · **承重不变量** · 硬规则 · 明确不做 | 每次 —— 自动注入 |
| `CONTEXT.md` | 术语 · 业务场景 · 上游数据形状 · 领域类型契约 · 词表生命周期 | 动契约、动语义之前 |
| `docs/architecture.md` | 七个模块各自内部 · 端口上什么不许出门 · MySQL 表键的理由 · webUI | 动某个模块之前 |
| `docs/adr/` | 有过取舍、被实测校准过的决策 | 想改一条既定做法之前 |
| `docs/status.md` | 待办 · 当前样本 · 还没定死的常量 | 想知道「现在做到哪了」 |

一条事实只写在一处，互相引用不复制。ADR 大多带实测数字 —— 要推翻先拿同等实测。

**现有 ADR**：0001 `_body` 脱敏只掩锚点确定的东西 · 0002 `replyTo` 用于抽取后对齐 ·
0003 ①~⑦ 不做成 agent · 0004 群内串行链传便签、并行只加在群之间 ·
0005 拉取靠索引表 + HTTP Range，本地是 OSS 的月文件镜像 ·
**0006 配置分 config.toml / secrets.toml 两个文件，不走环境变量**（Rust 版新增）。

## 承重不变量

这八条错一条就是静默的数据损坏。

### 1. 冻结：事实与标注分开

- **冻结区**（`occurred_on < T-2`）：事实列不可写；标注列只在**词表升版**时整体重来。
- **非冻结区**（`occurred_on ∈ [T-2, T-1]`）：事实列随便重写，**标注跟着事实自动重查**（因为 `classify` 是确定性的 + 有缓存）。

没有这条，删重写会制造**抖动而非修正**：同一批数据重跑，模型这次可能抽出 11 个 event 而不是昨天的 12 个。报表一旦会这样变，就不能用来做同比环比。

### 2. 两个分片必须同一个事务

一个群一次运行要写 `T-2` 和 `T-1` 两个分片。**一个事务。**

`occurred_on = date(first_msg_time)`，而"首条来源消息是哪条"由模型判断 —— 同一个 event 会在两个分片之间移动。分两个事务，中间失败就会造成这个 event **一个分片都不在**或**两个分片都在**。

### 3. 失败隔离 = 群 × 日

- 一个群本次运行的**所有窗口全部成功** → 整体重写该群两个分片。
- **任一窗口失败** → 该群整体跳过，**一行不写**，记入 `run_failure`。
- 整轮不因单群失败中止。运行结束汇报成功/失败群数。

少一个窗口就重写，等于**用残缺数据覆盖完整数据**，比不重写更糟。

### 4. `Ok([])` 与 `Failed` 绝不混淆

| 抽取结果 | `extraction_status` | 事件级指标 |
|---|---|---|
| `Ok([])`（这个群这天确实没有业务事件，正常） | `ok` | **0** |
| `Failed`（没算出来） | `failed` | **NULL** |

**绝不用 0 表示"没算出来"。**

### 5. `metric_agent_daily` 在失败的群上整行缺失

不是 0，是**没有这一行**（和 `event` 一致）。

判断某客服当天的数字完不完整 → join `metric_group_daily` 看当天有没有 `failed` 行。
**直接 `SUM` 而不检查这一点，会得到一个偏小但看起来正常的数字。**

### 6. 溯源

- `Event.source_msg_ids` 非空，且每个 ID 必须真实存在于该次抽取的消息里。
- **模型根本不接触 `msg_id`** —— prompt 里给的是窗口内序号，代码映射回 `sourceMessageId`。序号越界直接是校验失败。这从根本上消灭了"模型编造 msgid"这个失败模式。
- 序号↔`msg_id` 的映射是抽取模块的内部状态，不出现在任何接口上。

### 7. 正文脱敏

给模型的正文必须过 `_body`。**不是隐私洁癖，是三件事同时**：

1. **PII 出境** —— 群里没有终端消费者，但客户的姓名 / 手机号 / 门牌号级住址全在正文里，明文发到第三方端点。传输层只有 `store=False` 一道控制。
2. **正文冒充行框架** —— `render` 用 `\n` 拼行，正文里的 `\n` 与框架的 `\n` 不可区分。一条正文若含 `#12 [10:00:00] 平台A: 已取消`，模型无从分辨它是不是真的第 12 行，而 validator 只查序号越界不查归属 —— **这是承重不变量 6「溯源」的绕过路径**。折行之后「一条消息恒占一行」由构造保证（实测 `#N` 行数 == 消息数 == 1096）。
3. **顺序依赖** —— 见 **ADR-0001**。引用块先删、折行必须早于字段脱敏。

**只掩锚点确定的东西。** 姓名和自由文本地址一概不碰 —— 判据是「这个位置的东西**必然**是什么」，不是「**看起来像**什么」。看起来像的那条路试过，被 `改电话` 捅穿了；掩错的代价是把一个动作变成一个人，比留下 PII 更糟。

### 8. 标识体系

- **`agent` = `easyUserId`**（16 位定长，内外统一）。`officialUserId` 形态混杂（手机号 / 字母账号），**不可用作主键** —— 换号即腰斩。
- **`room` = `officialRoomId`**（= 文件名，路径可直接解析）。

人用 `easy` 系、群用 `official` 系，两套混用是**有意为之**（各取最稳的那个），不要"顺手统一"。

> ⚠️ `easyUserId` 在换号/离职后是否保持不变，**尚未确认**。见 `.scratch/wecom-chat-analytics/open-questions.md` 第 1 条。

---

---

## 硬规则

### 不把大数据集读进内存

过滤、投影、排序、分组尽量下推到适配器（今天是 DuckDB 的 SQL）里。

- 用 `read_json_auto(..., format='newline_delimited')` 直接扫描文件/glob，不预先反序列化成内存对象。
- **按群读**：`list_rooms()` 只遍历目录，`read_room()` 读一个群、放 `tokio::task::spawn_blocking`（DuckDB 是同步阻塞的；不挪出去的话每读一个群，N 个在飞的模型调用全被卡在同一个 runtime 线程上）。**内存上界由 `ROOM_CONCURRENCY` 那个 `tokio::sync::Semaphore` 保证**，不需要队列。
  ⚠️ **群级**并发还没搬过来 —— 今天 `daily.rs` 是串行 for 循环。（`pull` 自己是并发的，
  走 `JoinSet` 的背压 + `pull_concurrency`，那是下载不是读取，两回事。）
  ⚠️ **队列 / 背压 / 生产者线程已经删掉了，别再加回来。** 它们存在的唯一理由是「所有群混在一堆文件里、必须一次排序才能切开」；真实布局是**一个群一个月一个文件**，那个前提没了 —— R 次查询各碰各的几 MB，总 I/O 本来就是一遍。
- **禁止对全量表 `fetchall()` / `df()`。**
- ⚠️ **一条例外：消息级计数在进程内算，不下推 SQL。** 那个群的消息本来就已经在内存里了（③ 要用），这里没有多读一个字节。为它单开一条 SQL 等于**同一份数据扫两遍**，而且是逼着未来每个适配器都支持 SQL 的唯一理由。


### 群内串行、段内自适应 —— 论证见 **ADR-0004**

```
一个群一天  ──切成 ceil(n / SEGMENT_MSGS) 段──> 段1 →📝→ 段2 →📝→ 段K
                                                （全群共用一套 drafts，零接缝）
```

- **段之间必须串行**，后一段要看前一段的便签。**并行只加在群与群之间。**
- **整段先试一次，模型吃不下才对半切。**「是否切分」看运行时信号（`incomplete` / `APITimeoutError`），
  **不认 `APIConnectionError`**，**没有阈值参数**。切不动了（剩一条仍失败）→ 该群本日失败。
- **便签接住跨段事件**，不靠规则预防，也不靠按订单号事后合并 —— 订单号是「工单」的钥匙不是「事件」的钥匙。
  **段内被拆散的靠 `replyTo` 对齐**（ADR-0002）。
- **不重叠、不去重。** 每条消息恰好属于一段。
- `SEGMENT_MSGS`（省钱旋钮，不是质量旋钮）与 `ROOM_CONCURRENCY`（唯一能压的墙钟旋钮，约束是
  **TPM 不是并发数**）**都无默认值，缺失即报错**。
- ⚠️ **窗口条数上限 / 重叠量 / 静默阈值 / 线程装箱 / 块间并行 —— 已经不存在了，别再加回来。**

### 抽取实现必须可替换

模型名、API key、prompt 模板都是抽取模块的内部细节。换一个模型、或改成纯规则实现，改动应收敛在 ③ 内 —— 换模型只换 `SegmentModel` 的一个适配器。

### 模型输出必须先校验再落库

校验不通过 = 该批次失败。不做字段级兜底修补、不落库半个事件。

### 让程序错误显式暴露

`parserVersion` 不匹配 → 失败退出。配置缺字段 / 类型不对 → `config.rs` 直接 panic，不给默认值（配置错误要在进程起来的第一秒暴露，不是跑到落库那步才炸）。输出被截断 → 翻译成显式的「预算耗尽」再二分，**不能让它表现成一次 JSON 解析失败**。二分发生时打日志，静默的二分等于不知道自己的群在被切。不用静默兜底或多余的提前 return 掩盖。

**⚠️ 这一条在 Rust 里和 Python 版是反的，别照搬。** Python 那边的规则是「生产路径不用 `assert`」，
理由是 `python -O` 会把 `assert` 整行删掉。**Rust 没有这个问题** —— `assert!` / `assert_eq!`
在 release 下照样执行，被编译掉的只有 `debug_assert!`。所以：

- **承重不变量绝不用 `debug_assert!`。** 那才是会在 release 里蒸发的那个，跑批是无人值守的
  定时任务，守卫蒸发之后程序照样绿灯、数据默默写坏。
- **但「用什么表达失败」的判据换了一个**：不是「会不会被编译掉」，而是**失败隔离粒度**。
  承重不变量 3 要求「某个群失败 → 该群跳过一行不写，**整轮继续**」，而 panic 会掀掉整轮。
  所以 **群级 / 日级的失败一律走 `Result`**（`IngestError::Room` 就是这么分的），
  `panic!` / `unwrap` / `expect` 只留给两种地方：**进程启动时的配置与资源**（本来就该整轮死），
  以及**构造已经保证、不可能为假**的不变量（写 `expect` 时把「为什么不可能」写进消息里）。

受这条管的六处：来源消息 ID 非空（不变量 6）· 时间不倒挂 · 摘要长度 · `first_responder ∈ agents` · **事件不许落在窗口外（写穿冻结区，不变量 1）** · 归属口径合法。

---

---

## 明确不做

- **跑批**不引入 Kafka、Celery、Spark、向量库，或任何常驻服务。
- **webUI 是这张清单上唯一的例外，且只读** —— 不写任何表、不调模型、不参与跑批。设计见 `docs/architecture.md`。
- 不做在线/实时处理，不做增量订阅。**T+1 跑批是明确前提。**
- **不给 ① 加基类 / 工厂 / 注册表 / `SOURCE_TYPE` 配置项。** 换数据源靠新写一个适配器文件 —— 一个适配器 = 假想接缝。
- 不做多租户、权限、审计。`corpid` 只是 event 的一个属性，不做隔离机制。
- 不建「存储层抽象接口」。MySQL 是当前唯一目标，只保证写库代码集中在一处。
- 不做「解决时长」—— 需要先定义「什么算解决」。
- 不做词表管理界面、不做 ORM、不做 migration 框架。
- **不把 ①~⑦ 这条线做成 agent** —— 判据见 **ADR-0003**。

---

## 当前状态

⚠️ **这是 Rust 重写仓库（`chat2events-rs`），不是 Python 那份。** 七阶段、承重不变量、
领域契约、ADR 全部照搬 —— 那些是设计不是实现。**已搬过来的是 ①②**（拉取 + 摄取 + 会话）
和 LLM 调用层，③④⑤⑥⑦ 仍然只存在于 Python 版（`../pychat2events/`）。逐阶段的搬运状态见上面
七阶段表的「Rust 现状」列，细节与待办见 `docs/status.md`。

**Rust 版和 Python 版的三处实质差异**（不是翻译，是重新决定的）：

| | Python | Rust |
|---|---|---|
| 配置来源 | `os.environ` + `.env` | `config.toml` ＋ `secrets.toml`（0600 强制），见 **ADR-0006** |
| 承重不变量的表达 | 显式 `raise`（`assert` 会被 `-O` 删掉） | `Result` 为主；`assert!` 在 release 下不会消失，**`debug_assert!` 才会**。判据换成失败隔离粒度，见上面「让程序错误显式暴露」 |
| 并发 | `asyncio` + `ROOM_CONCURRENCY` semaphore | 群级还没搬（`daily.rs` 串行 for）；`pull` 已并发，走 `JoinSet` 背压 |

**已验证**：`cargo test` **29 个用例 / 0.8s**（`ingest` 20 + `pull` 5 + `llm` 1 + `window` 3）。
单元测试跟着被测代码走（`#[cfg(test)] mod tests`；`ingest` 的测试体积大，用 `#[path]`
分文件、逻辑上仍是同一模块，私有可见性不变）。lib + bin 拆分后 `tests/` 结构上可用，
但今天没有集成测试 —— `daily::run` 需要真实 MySQL 与端点。共享 fixture 在
`src/testutil.rs`（仅 `cfg(test)` 编译）。

⚠️ **样本会被就地替换**，文档里带条数的实测数字必须注明是哪一版样本量的。当前样本单群跨 5 天 3742 条（2026-08-25 ~ 08-29），**时间戳真实**。

待办 · 样本细节 · 还没定死的常量 → **`docs/status.md`**。
外部还没回答的六个问题 → `../pychat2events/.scratch/wecom-chat-analytics/open-questions.md`。

---

## Agent skills

### Domain docs

单上下文：根目录 `CONTEXT.md` + `docs/adr/`。
