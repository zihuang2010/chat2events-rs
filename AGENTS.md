# 全局工程规则

## 项目上下文

- 开始本项目任务前，先阅读 `CLAUDE.md`，了解项目架构、业务不变量、文档入口和 codebase-memory-mcp 检索规则；涉及具体约束时按其中指引阅读对应文档。

## 代码检索

- 优先使用 `codebase-memory-mcp` 检索代码；符号定位、调用链、架构和改动影响分析先查图，具体工具用法见 `CLAUDE.md` 的「检索代码」章节。
- 字面量、配置和非代码内容优先使用 `search_code`；图不覆盖的内容用 `rg` 或直接读文件核查。
- 图中未命中不代表代码不存在；作出不存在或完整影响范围的结论前，调用 `check_index_coverage`，并对缺失、过期或部分索引的范围核查源码。
- MCP 工具不可用时，先说明限制，再使用 `rg` 和源码读取完成检索；报告依据为源码，不能声称已查询或更新图索引。

## 语言

- 一律用中文回复,除非用户明确要求其他语言。
- commit message 用中文。
- 新增代码注释用中文;若所改文件已统一使用英文注释,则跟随英文。
- 标识符一律用英文:函数名、变量名、类型名,**测试函数名也不例外**。中文只出现在注释、文档字符串和字面量里。

## 实现哲学

遵循 KISS 与 YAGNI:只实现当前需求要的东西,用最小的合理改动,尽量少动文件。

- 优先复用代码库里已有的模式,而不是引入新的架构层。
- 少量重复优于错误的抽象。
- 抽象、扩展点、兼容层、兜底逻辑、配置项:等到有真实的第二个用例再加。
- 防御性代码只写在真实的信任边界上(外部输入、跨进程、用户数据)。内部可信代码已保证的不变量,不重复校验。
- 让程序错误显式暴露(抛异常 / 报错),不用静默兜底或多余的提前 return 掩盖。
- 需求有歧义、或涉及未言明的业务逻辑:先问,不猜。
- 有更简单的方案时明确说出来,不默默选复杂的那个。

引入额外复杂度之前,先自问四题:

1. 当前需求是否真的要求它?
2. 有证据表明这个边界情况真会发生吗?
3. 这个抽象是否降低了当前的复杂度?
4. 同样的需求能否用更少的代码满足?

只要有一题答不上,就选更简单的实现。

## 改动范围

- 只改当前任务需要的代码,不做顺手重构、改名、格式统一。
- 途中发现的坏代码/死代码/隐患:单独报告给用户。除非它挡住了当前改动,否则不顺手修。
- 非平凡任务开工前,先说清四件事:需要的行为、最小的合理实现、要改哪些文件、明确不做什么。
- 把改动分成 REQUIRED 和 OPTIONAL。默认只做 REQUIRED,OPTIONAL 列出来等用户拍板。

## Git 安全

- 不主动 commit / push / 建 PR,除非用户明确要求。
- 禁止 force push、rebase 已推送的分支、`git reset --hard`、用 checkout 覆盖未提交改动。
- 不直接向 master / main 提交,先建分支。
- 不提交密钥、token、.env 文件。

## 验证

- 改完说明怎么验证的:跑了什么命令、结果如何。没跑就直说没跑。
- 没有实际运行结果,不说"已修复""已验证"。
- 测试失败照实报告并附输出,不淡化。
